# certificates-vault
Platform storing and displaying skills certificates

# Installation
Create .env config file using .env.example
Then run in console:
```
composer install
php artisan migrate
npm install
npm run build
```